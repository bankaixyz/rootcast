export default function HomePage() {
  return (
    <main style={{ padding: "3rem", fontFamily: "sans-serif" }}>
      <h1>World ID Root Replicator</h1>
      <p>
        Phase 1 scaffolds the read-only frontend shell. Later phases will turn
        this into a dark landing page and replication dashboard.
      </p>
    </main>
  );
}
